{
  "buildCommand": "ng build --configuration=production",
  "outputDirectory": "dist/browser",
  "framework": "angular",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
